# Support Matrix

- PHP: 8.2, 8.3, 8.4 (CI matrix)
- HTTP: any PSR-18 client + PSR-17 factories (tested with nyholm/psr7 + curl-client)
- OS: Linux/macOS/Windows (no OS-specific code)
- Upstream API: go-whatsapp-web-multidevice REST OpenAPI 6.12.0 + webhook payloads (manifest tracked in `upstream/manifest.json`)
